// Code your solution in this file!
function distanceFromHqinBlocks(a){
    return a 
   
}
function distanceFromHqinFeet(b){
   distanceFromHqinBlocks();
   return a * b

}
function distanceTravelledInFeet(start, destination){
    return start + destination
}
function calculatesFarePrice(){

    if(b<=400)[
       fare = 0 
    ]
    if (b<=2000){
        fare = 2 * b
    }
}


distanceFromHqinBlocks()
distanceFromHqinFeet()
distanceTravelledInFeet()
calculatesFarePrice()
 // Code your solution in this file!
 function distanceFromHqInBlocks(a) {
     return Math.abs(a - 42); // Assuming 42 is the block number of 42nd Street
 }
 
 function distanceFromHqInFeet(b) {
     const blocksFromHq = distanceFromHqInBlocks(b);
     return blocksFromHq * 264; // Assuming 1 block is 264 feet
 }
 
 function distanceTravelledInFeet(start, destination) {
     const blocksTravelled = Math.abs(start - destination);
     return blocksTravelled * 264; // Assuming 1 block is 264 feet
 }
 
 function calculatesFarePrice(start, destination) {
     const distanceInFeet = distanceTravelledInFeet(start, destination);
 
     if (distanceInFeet <= 400) {
         return 0;
     } else if (distanceInFeet <= 2000) {
         return 2 * distanceInFeet;
     } else {
         return 25;
     }
 }
 
 distanceFromHqInBlocks(43);
 distanceFromHqInFeet(43);
 distanceTravelledInFeet(34, 50);
 calculatesFarePrice(34, 50);// Code your solution in this file!
 function distanceFromHqInBlocks(a) {
     return Math.abs(a - 42); // Assuming 42 is the block number of 42nd Street
 }
 
 function distanceFromHqInFeet(b) {
     const blocksFromHq = distanceFromHqInBlocks(b);
     return blocksFromHq * 264; // Assuming 1 block is 264 feet
 }
 
 function distanceTravelledInFeet(start, destination) {
     const blocksTravelled = Math.abs(start - destination);
     return blocksTravelled * 264; // Assuming 1 block is 264 feet
 }
 
 function calculatesFarePrice(start, destination) {
     const distanceInFeet = distanceTravelledInFeet(start, destination);
 
     if (distanceInFeet <= 400) {
         return 0;
     } else if (distanceInFeet <= 2000) {
         return 2 * distanceInFeet;
     } else {
         return 25;
     }
 }
 
 distanceFromHqInBlocks(43);
 distanceFromHqInFeet(43);
 distanceTravelledInFeet(34, 50);
 calculatesFarePrice(34, 50);




